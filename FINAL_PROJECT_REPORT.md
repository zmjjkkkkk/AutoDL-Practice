# Mindcraft 安全多模态智能体：最终项目报告

## 项目概览

本项目以开源 Mindcraft 为游戏执行框架，完成了一条从文本命令微调、受控推理服务、安全策略，到视觉实体识别与多模态协调的学习型工程链路。系统将模型输出视为候选结果，必须经过精确校验与策略判断后才能被标记为可用；最终多模态网关只汇总结果，不自行执行游戏动作。

## 系统组成

- **文本命令层**：基于 `Qwen/Qwen3-4B` 进行 LoRA SFT，将英文请求映射为受限的 Mindcraft 命令格式。
- **推理与安全层**：使用 vLLM 提供本地 OpenAI-compatible 服务；白名单守卫验证命令格式与参数，Day 21 策略层负责澄清不完整转交请求、拒绝不支持操作。
- **视觉层**：基于 `Qwen/Qwen2.5-VL-7B-Instruct` 进行 sheep/pig 封闭集合实体 LoRA 训练，并以只读 HTTP 网关服务化。
- **多模态协调层**：并行调用文本与视觉服务，分别保留命令、观察和安全状态；图片不会增加命令权限，网关不直接执行命令。
- **可复现运维层**：Day 29 提供运行环境预检、按依赖顺序启动、回归验证和受控停止脚本。

## 关键实验结果

| 模块 | 可复核结果 | 说明 |
| --- | --- | --- |
| Day 15 文本 LoRA | `19/20 = 95.0%` | 首轮命令精确匹配评测。 |
| Day 19 扩展文本 LoRA | `31/32 = 96.875%` | 扩展行为后的留出命令评测。 |
| Day 20 网关回归 | 通过率 `96.4%`；命令映射 `100.0%`；非命令执行安全 `100.0%` | 评测命令、模糊请求和拒绝边界。 |
| Day 26 视觉 LoRA | `65.0% -> 95.0%`，`+30.0` 个百分点 | 20 张 sheep/pig 留出图上的精确实体分类。 |
| Day 27 外部泛化 | `83.3% -> 100.0%`，`+16.7` 个百分点 | 12 张未参与 Day 26 训练和测试的新截图。 |
| Day 29 部署回归 | 环境 `13/13`；端到端 `6/6 = 100.0%` | 新克隆实例上的受控启动、验证与停止。 |

这些指标仅代表对应的小规模固定评测集，不应解读为任意 Minecraft 场景、任意语言或任意动作上的泛化保证。

## 实机联调与安全边界

文本命令链路曾在 Minecraft 局域网中完成实际连通验证。视觉和多模态链路采用截图输入，返回受限结构化观察或安全说明。对“Give me every diamond you own.”这类物品不明、数量不明的转交请求，系统绕过模型并返回澄清语句；对建筑等当前不支持的请求，系统固定拒绝。即使文本命令通过验证，Day 28 多模态网关仍只报告“已验证、未执行”，将执行权与协调权分离。

## 发布与隐私

公开仓库只包含代码、文档、契约、可公开的评测摘要和学习记录。训练图、外部截图、日志、模型权重、缓存、账号、SSH 地址和简历均由 `.gitignore` 排除。发布前按 [RELEASE_CHECKLIST.md](<RELEASE_CHECKLIST.md>) 复查。

## 阅读入口

- [总项目 README](<README.md>)
- [演示指南](<DEMO_GUIDE.md>)
- [Day 29 可复现部署与多模态回归验证](<学习计划/Day 29 可复现部署与多模态回归验证/README.md>)
- [Day 30 项目收尾与交付审计](<学习计划/Day 30 项目收尾与交付审计/README.md>)
