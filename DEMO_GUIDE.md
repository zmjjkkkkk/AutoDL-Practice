# 演示指南

本指南面向答辩或课堂展示。演示不需要展示 API Key、SSH 地址、私有截图、训练数据或模型权重。

## 演示前

1. 展示 [FINAL_PROJECT_REPORT.md](<FINAL_PROJECT_REPORT.md>) 中的系统架构与固定评测结果。
2. 仅使用可公开的示例请求；截图演示请使用自己的可公开画面。
3. 如需启动远端服务，先按 Day 29 `RUNBOOK.md` 完成预检；演示结束后运行 `stop_stack.sh`。

## 三分钟流程

1. **服务健康检查**：展示文本、视觉与多模态服务均可达。
2. **视觉只读观察**：上传一张包含 sheep 或 pig 的截图，展示封闭集合实体结果；强调它不产生游戏动作。
3. **文本命令验证**：输入 `please follow me`，展示候选命令被守卫验证；在 Day 28 中它只会被报告为已验证、未执行。
4. **安全边界**：输入 `Give me every diamond you own.`，展示系统绕过模型并要求提供受支持物品和正整数数量。
5. **结束与清理**：展示 Day 29 回归 `6/6` 和受控停止结果；停止服务后确认 GPU 无残留进程。

## 演示要点

- 模型不是执行器：候选输出需要通过策略和命令守卫。
- 视觉不是权限来源：图片结果不能扩张允许执行的命令范围。
- 指标对应固定测试集：准确率说明实验结果，不夸大为通用能力。
